# Backend API base URL (must be reachable from mobile device/emulator)
EXPO_PUBLIC_API_URL=http://localhost:3003

# Optional websocket base URL (if different from API URL)
# EXPO_PUBLIC_WS_URL=http://localhost:3003
