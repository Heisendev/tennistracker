# Frontend environment variables
# In production (Vercel), set this to /api — requests are proxied to Render via vercel.json
# In development, set this to http://localhost:3003
VITE_API_URL=http://localhost:3003
